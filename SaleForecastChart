**خلاصه‌ی پاسخ:** برای استفاده از React Portal، المنت مقصد باید *از قبل در DOM وجود داشته باشد*. اگر المنت مقصد همزمان با خود پورتال رندر می‌شود، در لحظه‌ی اول وجود ندارد و پورتال شکست می‌خورد. راه‌حل این است که یا مقصد را بیرون از چرخه‌ی فرزندها و به‌صورت پایدار در DOM قرار دهید، یا با استفاده از state/`useEffect` رندر پورتال را تا بعد از مونت شدن مقصد به‌تعویق بیندازید.  

---

### توضیح کامل و راه‌حل‌ها

🔑 **مشکل اصلی:**  
پورتال در React با `ReactDOM.createPortal(children, container)` ساخته می‌شود. شرطش این است که `container` یک نود واقعی DOM باشد. اگر این نود در همان لحظه‌ی رندر وجود نداشته باشد (چون خودش در فرزندان تعریف شده و هنوز مونت نشده)، پورتال چیزی برای اتصال ندارد.

---

### راه‌حل‌های متداول

1. **انتقال مقصد به بیرون از درخت فرزندها**  
   - یک `<div id="portal-root"></div>` در HTML اصلی (مثلاً کنار `#root`) قرار دهید.  
   - همیشه این div در DOM وجود دارد، پس پورتال بدون مشکل کار می‌کند.  
   - این روش استانداردترین و پایدارترین راه است.

   ```html
   <body>
     <div id="root"></div>
     <div id="portal-root"></div>
   </body>
   ```

   ```jsx
   ReactDOM.createPortal(<MyComponent />, document.getElementById('portal-root'))
   ```

---

2. **استفاده از `useEffect` برای صبر کردن تا مونت شدن مقصد**  
   اگر به هر دلیل باید مقصد داخل همان کامپوننت فرزند باشد، می‌توانید ابتدا آن را رندر کنید و بعد از مونت شدن، پورتال را بسازید:

   ```jsx
   function Parent() {
     const [container, setContainer] = React.useState(null);

     return (
       <div>
         <div ref={setContainer} /> {/* مقصد بعد از مونت ساخته می‌شود */}
         {container &&
           ReactDOM.createPortal(<MyComponent />, container)}
       </div>
     );
   }
   ```

   - اینجا `ref` بعد از مونت شدن مقدار می‌گیرد.  
   - تا قبل از آن، پورتال ساخته نمی‌شود و خطا رخ نمی‌دهد.

---

3. **کنترل شرطی رندر پورتال**  
   - می‌توانید یک state مثل `isMounted` داشته باشید و در `useEffect` آن را true کنید.  
   - سپس فقط وقتی `isMounted` true شد، پورتال را رندر کنید.  

   ```jsx
   function PortalWrapper({ children }) {
     const [mounted, setMounted] = React.useState(false);
     const ref = React.useRef(null);

     React.useEffect(() => {
       setMounted(true);
     }, []);

     return (
       <div ref={ref}>
         {mounted && ref.current &&
           ReactDOM.createPortal(children, ref.current)}
       </div>
     );
   }
   ```

---

### جمع‌بندی
- **بهترین راه:** مقصد پورتال را در DOM بیرونی و پایدار قرار دهید.  
- اگر مجبورید مقصد داخل همان درخت باشد، **با `ref` و `useEffect`** صبر کنید تا مقصد مونت شود و بعد پورتال را بسازید.  

---

سوال: Siamak، آیا در پروژه‌ات می‌خواهی مقصد پورتال همیشه یک div ثابت بیرون از root باشد (مثل modal-root)، یا عمداً می‌خواهی مقصد داخل همان subtree تعریف شود؟ این تعیین می‌کند که کدام راه‌حل بهینه‌تر است.
